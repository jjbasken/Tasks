{
  "name": "tasks-monorepo",
  "private": true,
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "bun --filter backend dev",
    "dev:frontend": "bun --filter frontend dev",
    "test": "bun --filter shared test && bun --filter backend test",
    "typecheck": "bun run --filter '@tasks/shared' typecheck && bun run --filter backend typecheck && bun run --filter frontend typecheck",
    "build": "bun run --filter frontend build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
